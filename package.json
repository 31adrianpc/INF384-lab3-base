{
  "name": "inf384-lab3",
  "version": "1.0.0",
  "private": true,
  "description": "Funcion Lambda de ejemplo del Laboratorio 3 de INF384.",
  "main": "src/handler.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "esbuild src/handler.js --bundle --platform=node --target=node20 --outfile=dist/handler.js"
  },
  "dependencies": {
    "tough-cookie": "4.1.2"
  },
  "devDependencies": {
    "esbuild": "0.21.5",
    "jest": "29.7.0"
  }
}
